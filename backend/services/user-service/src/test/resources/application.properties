# Hardened test configuration for CI environment
# Use the %test prefix to ensure these properties take precedence during 'mvn test'

%test.quarkus.datasource.db-kind=h2
%test.quarkus.datasource.jdbc.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
%test.quarkus.hibernate-orm.database.generation=drop-and-create

# Disable security and external integrations during tests to avoid boot failures
%test.quarkus.oidc.enabled=false
%test.quarkus.keycloak.admin-client.server-url=http://localhost:8081
%test.quarkus.mailer.mock=true
