services:
  processengine:
  # image: ghcr.io/thi-projekte/handwerker/processes:latest
    build: .
    restart: unless-stopped
    environment:
      SERVER_FORWARD_HEADERS_STRATEGY: framework
    ports:
      - "${PROCESSENGINE_HTTP_PORT:-8080}:8080"
    networks:
      - default
      - nginx-proxy-manager

networks:
  nginx-proxy-manager:
    external: true